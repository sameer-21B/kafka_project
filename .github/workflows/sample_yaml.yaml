name : Sample yaml file to test actions

on:
  push:
    branches:
      main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name : Sample script print
        run: |
          echo "actions is running fine."